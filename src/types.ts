export interface Portfolio {
  owner_name: string;
  owner_role: string;
  company: string;
  link: string;
  showcase_images: string[];
}

export interface CompanyColor {
  bg: string;
  text: string;
}
